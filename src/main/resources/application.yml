
server:
  port: 8888

spring:
  application:
    name: spring-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LauroSilveira/config-file.git
          username: LauroSilveira
          password: zozza4-qafxeh-nyDgav

management:
  health:
    refresh:
      enabled: true